# Sonar Data Collection - Python Dependencies
# Install: pip install -r requirements.txt

# Database
supabase>=2.10.0
python-dotenv>=1.0.0

# HTTP Clients
httpx>=0.27.0
requests>=2.31.0
cloudscraper>=1.2.71
curl_cffi>=0.6.0

# Event loop em C (drop-in replacement do asyncio). 20-30% speedup em
# codigo I/O-bound. Apenas Linux/macOS — o entrypoint scrapy.py importa
# com try/except, no Windows cai pro asyncio default sem quebrar.
uvloop>=0.19.0; sys_platform != "win32"

# HTML Parsing
beautifulsoup4>=4.12.0

# Browser Automation (for Google Enricher)
playwright>=1.40.0

# Traducao offline de vagas estrangeiras (engine careerjet multi-pais).
# Roda modelos localmente, sem chamada de rede; baixa os modelos sob
# demanda (~100-250MB por par de idiomas) no primeiro uso.
argostranslate>=1.9.0

# Env loading (.env)
python-dotenv>=1.0.0

# Testes - pytest-asyncio habilita os testes async (test_core_sink usa
# @pytest.mark.asyncio; sem o plugin esses casos falham na coleta).
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Standard library dependencies already included in Python:
# - asyncio
# - json
# - csv
# - os
# - sys
# - re
# - logging
# - datetime
# - typing
# - pathlib
# - unicodedata
# - xml.etree.ElementTree
# - urllib.parse
# - http.server
# - threading
# - importlib
# - inspect
