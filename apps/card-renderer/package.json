{
  "name": "sonnar-card-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel Edge Function que gera os cards 1080x1080 das vagas Sonnar via @vercel/og (Satori). Substitui o processo sonnar-wa-formatter da VPS.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/og": "^0.6.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^18.3.12",
    "typescript": "^5.6.3",
    "vercel": "^39.1.3"
  }
}
